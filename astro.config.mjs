// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Required for absolute URLs in the RSS feed and canonical/OG tags.
  site: 'https://pedrocastro.eu',
});
